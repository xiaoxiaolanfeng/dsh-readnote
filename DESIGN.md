# readnote · 设计稿 v1

> **一句话**：把 AI 的回答**钉回原文**的文档阅读器。
>
> **中文描述**：阅读增强 —— 边读文档，边批注，边问 AI。

---

## 1. 它解决什么

你读一份长文档（markdown），现在有三个需求没人满足：

| 场景 | 现状 |
|---|---|
| 读到不懂的地方想问 AI | 得复制粘贴、切窗口，读的节奏断了 |
| 有个想法想记在这段文字边上 | 没地方记；记了下次也翻不到 |
| AI 给了个好答案 | 沉在对话历史里，翻不回来 |

**这三个都指向同一件事：读文档时产生的思考，没有落脚点。**

---

## 2. 为什么没人做过（实测调研结论）

装了 7 个生态里最相关的插件实测，加一轮生态扫描：

- 划词 / 批注 / 阅读类插件，生态里**至少 9 个**
- **全部以「AI 回复」为划词对象，没有一个以「文档」为对象**
- star 分布：`dsh-annotation` 114★（唯一过 30 的功能插件），其余全部 0–7★

实测两个结论（这是本项目的立项依据）：

- **A · 入口不可发现** —— 插件装上了、跑起来了、产物完整，但找不到入口。产品上等于不存在。
- **B · 对象错了** —— 能触发的插件，划词对象全是聊天流。够不着你真正在读的东西。

> 这不是技术做不到，是**设计起点不同**：dsh 是 agent harness，世界中心是"对话"，
> 所以插件作者们自然都围着对话做。没有人从"我在读一份文档"出发。

---

## 3. 界面形态

`conversation.view` 主视图页签，与「对话 / 轨迹」并列：

```
┌──────────────────────────────────────────────────────┐
│  [对话]  [轨迹]  [阅读]                     ← order 20│
├───────────────────────────────┬──────────────────────┤
│                               │  问答面板             │
│   ┌───────────────────────┐   │  ──────────────      │
│   │                       │   │  💬 关于「Cordis…」   │
│   │   文档（markdown      │   │  这里为什么这么设计？  │
│   │   原生渲染）           │   │                      │
│   │                       │   │  AI: ……（流式）       │
│   │   划词 → 浮出工具条    │   │                      │
│   │   [记一笔] [问 AI]     │   │  ┌────────────────┐  │
│   └───────────────────────┘   │  │ 输入框          │  │
│                               │  └────────────────┘  │
├───────────────────────────────┴──────────────────────┤
│  [dsh 原本的 composer]                                 │
└──────────────────────────────────────────────────────┘
         ↑ 中间分栏可拖拽调宽
```

**两条从别人失败里学来的设计铁律：**

1. **入口必须显眼** —— 页签天然在「对话/轨迹」旁边，用户不可能看不见（治 A）
2. **对象必须是文档** —— 划词的对象是你打开的文件（治 B）

---

## 4. 三个动作

划词后浮出工具条，两个去向；AI 回答之后还有第三个动作，**由你决定要不要做**：

| 动作 | 触发点 | 谁写的 | 结果 |
|---|---|---|---|
| **记一笔** | 划词 | 只有你写 | 永久挂在原文上 |
| **问 AI** | 划词 | 你问，它答 | 进问答面板 |
| **钉成笔记** | AI 回答旁 | 提问是你，提炼是 AI | **你决定**：插入原文 or 只存着 |

**批注不触发 AI。** 强制让 AI 回答每条批注，会让笔记被淹没，也会让批注变重——你不再会随手记。
**批注的价值恰恰在于轻。**

### 钉成笔记：不是搬运，是提炼

AI 回答之后可以点「生成笔记」。关键区别：

| | 钉原话 | 生成笔记 |
|---|---|---|
| 钉上去的东西 | 500 字的 AI 回答 | 50 字的笔记 |
| 原文旁边会变成 | 被淹没 | 一条真正的页边注 |
| 动作的本质 | 搬运 | **提炼** |

**一条页边注应该是短的。** 你书上写的边注不会是整段抄录，而是"这里的关键是 X"。
所以钉的不是回答，是**提炼后的笔记**。

### 笔记的形态：两段，来源分明

```
✨ 笔记
┌────────────────────────────────┐
│ ❓ 你当时问的（原话，不改写）      │   ← 你写的
│   为什么 Cordis 的插件加载…       │
│                                │
│ 📝 提炼（可编辑）                │   ← AI 写的
│   插件加载走 Loader 的 EntryTree… │
│                                │
│ ▸ 查看原始回答                   │   ← 折叠：浓缩给你读，原文给你查
└────────────────────────────────┘
```

**保留原始提问** —— 问题是你的，答案是 AI 的。

现在的 AI 阅读工具全都只存答案，不存问题。但提问本身就是**你的理解地图**：
三个月后翻回来，"为什么 Cordis 的插件加载要绕这么大一圈？"比一段抽象结论有用得多，
因为它标出了你当时的认知边界。**只存答案，等于把自己那一半丢了。**

### 插入是可选的，不是必然

```
AI 回答 ──→ [生成笔记] ──→ 笔记草稿（可编辑）
                              │
                    ┌─────────┴─────────┐
                    ↓                   ↓
              [插入原文]            [只存着]
          挂到那段话旁边        留在问答面板里备查
```

- **重要概念** → 值得钉到原文，以后翻到就能看见
- **一时疑问** → 生成个笔记存档就行，不必占原文的位置

### 两条硬约束

1. **先看能改** —— 笔记生成后是草稿，你改完才落定
2. **不能直接写进原文** —— AI 不许在你的文档上自动写入

### 原文旁的三种东西，图标一眼分清

| 图标 | 来源 | 谁写的 |
|---|---|---|
| 🖊️ | 记一笔 | 你 |
| ✨ | AI 提炼的笔记（插进来的） | 提问是你，提炼是 AI |
| 💬 | 问 AI（还在面板里，没钉） | 对话 |

**来源必须能分清。** 不然三个月后翻回来，会分不清哪句是自己想通的、哪句是 AI 喂的——
这不只是洁癖，是**信任**问题。

> **「钉」这个动作就是学习的动作**：从一条流动的对话里，把有价值的东西固化到原文上。
>
> 现有 AI 阅读工具（NotebookLM 那一类）都是"你问它答，答完沉进历史"——**对话是消耗品**。
> 多一个"钉"，对话就变成了**资产**。

### 由此得到的定位

> **dsh-annotation 把批注当成"给模型的一次性输入"；readnote 把批注变成"文档上的资产"。**

---

## 5. 数据模型

| 数据 | 存在哪 | 为什么 |
|---|---|---|
| **文档内容** | 文件系统（`.md`） | 不进会话日志——它是输入，不是对话 |
| **批注** | 会话事件 | 见下方技术决策 |
| **问答** | 会话消息 | 复用 dsh 原生消息通道 |
| **锚点** | 批注事件的一部分（结构 id + 选中文本 quote） | 双重定位，改版不崩 |

**阅读视图不自己存数据**——它是会话日志的一个**投影**（和 Trajectory 视图同一思路）。

---

## 6. 关键技术决策

### 6.1 markdown 原生渲染，不用 iframe

支持 markdown 之后，前面分析过的"最难的技术坎"直接消失：

| | iframe 里读 HTML | 原生渲染 markdown |
|---|---|---|
| 拿选区 | 跨边界通信，要自己搭通道 | **原生，`selection` 直接就有** |
| 划词工具条定位 | 要映射坐标 | **原生** |
| 批注锚点 | 要设计跨边界协议 | **DOM 节点上直接挂** |
| 高亮 / 折叠 / 悬停 | 基本做不到 | **全都能做** |

而且 markdown 才是 AI 时代的主格式：AI 的输出、笔记导出、GitHub 文档、**dsh 自己的 skill 和 AGENTS.md 全是 markdown**。

### 6.2 批注落成会话事件（"Model-visible ⟺ logged"）

dsh 的架构铁律：**任何进入模型请求的东西，都必须能从会话日志重建。**

批注一旦参与"划词问 AI"，选中的文本就进入了模型请求——所以它不能躺在自己的小数据库里，
必须作为 session event 落进会话日志。

### 6.3 复用 dsh 原生机制，不碰核心

- 视图：`ctx.slots.inject('conversation.view', ...)` 注册页签
- 注册：官方 bundle plugin（`dsh.bundle` + `dsh.client`），**零核心改动**
- 输入：`agent.followup()` 把问题送进当前会话
- 样式：走 dsh 的 `--dsw-*` token，产品文案中文

---

## 7. 这一版不做什么

范围控制，明确排除：

- ❌ **PDF / HTML / epub** —— 架构上留渲染器接口，这一版只实现 markdown
- ❌ **系列 / 教学大纲** —— 以后再说（原本是另一个方向的需求）
- ❌ **生成器（模式一喂文章 / 模式二自动研究）** —— 与 beautiful-article 重叠，砍掉
- ❌ **部署 / 多人协作 / 公开发布站点**
- ❌ **划词的浮层做成小气泡** —— 会重蹈 A 的覆辙，入口必须显眼

---

## 8. 命名

```
npm 包名      dsh-readnote      (npm / GitHub 零占用)
插件 id       readnote
页签文案       阅读
中文描述       阅读增强：把 AI 的回答钉回原文的文档阅读器
仓库名        dsh-readnote
```

---

## 9. 最小验证（做完这个就算能跑）

一条纵向切片，端到端打通：

> **打开一篇 markdown → 划词 → 记一笔 → 关掉 dsh 重开 → 批注还在原位。**

就这一条，但会自然带出所有真问题：

- 阅读器挂哪 → `conversation.view` 主视图
- 文档怎么渲染 → 原生 markdown 渲染
- 锚点怎么定位 → 结构 id + 文本 quote
- 数据存哪 → 会话事件族

**五个技术点，一条线串起来。** 面试讲 30 分钟绰绰有余，每一层都是 dsh 的真实机制。

---

## 附：开发方式备忘

- **装别人的插件要重启，开发自己的插件不用**：改 `cordis.patch.yml`（用绝对路径 insert 插件源码）
  会触发 recompose；改 `package.json` 的 bundles 需要重启（已实测）
- **从 GitHub 直装插件有坑**：源码包没有 `lib/` 构建产物会直接崩（clearview 实测）
  → 发布时走 npm（带 lib/），或写 `prepare` 脚本
- 本机环境：dsh 0.1.5-rc.1，Node 22 + pnpm 12.4.1，git 已配 github-only 代理（7890）
